@*@model IEnumerable<Tag>
<div class="tn-box tn-widget tn-widget-content tn-corner-all spb-follow-topic">
    <div class="tn-box-header tn-widget-header tn-corner-top tn-border-bottom">
        <div class="tn-option">@Html.Link("添加", SiteUrls.Instance()._AddFollowTopic(Url.SpaceKey()), "添加", new { plugin = "dialog" })</div>
        <h3 class="tn-helper-reset">
            我关注的话题</h3>
    </div>
    <div class="tn-box-content tn-widget-content tn-corner-bottom">
        <ul class="tn-list">
            @foreach (var item in Model)
            {
                <li><a href="@SiteUrls.Instance().MicroblogTopic(item.TagName)" target="_blank">@item.TagName</a></li>
            }
        </ul>
    </div>
</div>
*@